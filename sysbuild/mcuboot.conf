# Surface MCUboot/bootutil progress logs (image validation, slot erase/copy)
# on the console. Without this an OTA upgrade boot is silent for ~15-20 s,
# which is indistinguishable from a bootloader hang.
CONFIG_MCUBOOT_LOG_LEVEL_INF=y
